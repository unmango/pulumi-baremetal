name: provider-baremetal-native
runtime: yaml
plugins:
  providers:
    - name: baremetal
      path: ../../bin

resources:
  myRandomResource:
    type: baremetal:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
