name: provider-baremetal-native
runtime: yaml
plugins:
  providers:
    - name: baremetal
      path: ../../bin

resources:
  tee:
    type: baremetal:cmd:Tee
    properties:
      stdin: 'whoops'
      create:
        files: ['/tmp/tee/test.txt']

outputs:
  stdout: ${tee.stdout}
